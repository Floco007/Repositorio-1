name: Verificar site
on: [push]
jobs:
    Verificar:
        runs-on: ubuntu-latest
        steps:
         - uses: actions/checkout@v4
         - name: Verificar intex.html
           run: test -f intex.html